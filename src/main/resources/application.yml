spring:
    application:
        name: crediCore

    datasource:
        url: jdbc:postgresql://localhost:5433/crediCore
        username: user
        password: "1234"

    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true

server:
    port: 9090